#pragma once


//#include <Windows.h>
//#include <afxwin.h>

/*
void win32_test()
{
	HWND hwnd = (HWND)0x00070E3C;

	HINSTANCE hInstance = (HINSTANCE)GetWindowLong(hwnd,GWL_HINSTANCE);

	HDC hdc = GetDC(hwnd);

	CWnd* cwnd = CWnd::FromHandle(hwnd);

	CString s;
	cwnd->GetWindowTextW(s);
}

int main_win32()
{
	win32_test();
	return 0;
}
*/